# Default backend: append-only terminal lines — one per epoch, never redrawn,
# and mirrored into `output_dir/training.log`.
# Zero external services, works offline.
defaults:
  - base_tracking
  - _self_

backend: console
