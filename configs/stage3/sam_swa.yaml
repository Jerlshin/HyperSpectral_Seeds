# Stage 3 — Sharpness-Aware Minimisation + greedy SWA snapshotting.
# Source: CONFIG["s3_*"] (monolith lines 115-120).
defaults:
  - base_stage3
  - _self_

epochs: 120
swa_lr: 4.0e-5
cycle_len: 8
sam_rho: 0.015
greedy: true
aux_loss_weight: 0.10
